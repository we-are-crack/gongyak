doctype html
html(lang="ko")
  include partials/head.pug
  body.bg-gradient-to-br.from-purple-50.to-gray-100.min-h-screen.flex.items-center.justify-center
    .container.max-w-5xl.w-full.px-20.py-20.flex.flex-col.items-center

      //- 로고/아이콘
      .flex.justify-center.mb-4
        svg.w-14.h-14.text-purple-600(fill="none", stroke="currentColor", stroke-width="2.2", viewBox="0 0 48 48")
          circle(cx="24", cy="24", r="22", stroke="#7c3aed", stroke-width="3", fill="#ede9fe")
          path(d="M16 24h16M24 16v16", stroke="#7c3aed", stroke-width="3", stroke-linecap="round")

      //- 제목
      h1.text-4xl.md-text-5xl.font-extrabold.text-gray-900.mb-4.tracking-tight.leading-tight.text-center
        | 21대 대선 후보자 
        span.text-purple-600 공약 비교
        |  서비스
      p.text-lg.text-gray-600.mb-8.font-medium.text-center.flex.flex-col.gap-1
        span
          | 신뢰할 수 있는 
          span.text-purple-700.font-semibold.font-bold AI가
          |  각 후보자의 공식 정책 공약집을 바탕으로
        span.text-purple-700.font-semibold 쉽고 명확하게 비교해드립니다.
        
      include partials/nav.pug

      //- 검색 창
      //- .search-box.flex.justify-center.mb-8.w-full#initialSearchBox
      //-   input#searchInput.p-4.w-full.max-w-md.rounded-l-xl.border.border-gray-300.focus-outline-none.focus-ring-2.focus-ring-purple-400.text-lg.shadow-sm.transition(type="text", maxlength="50", placeholder="예: 청년 주거 정책이 궁금해요")
      //-   button#searchButton.p-4.bg-purple-600.text-white.rounded-r-xl.hover-bg-purple-700.flex.items-center.gap-2.font-semibold.transition
      //-     svg.w-5.h-5(fill="none", stroke="currentColor", stroke-width="2.2", viewBox="0 0 24 24")
      //-       circle(cx="11", cy="11", r="7", stroke="currentColor")
      //-       path(d="M21 21l-3.5-3.5", stroke="currentColor", stroke-linecap="round")

      //- 검색 결과
      #results.results.space-y-4.w-full
        //- 동적으로 생성된 콘텐츠가 여기에 렌더링됩니다

      include partials/footer.pug
      include partials/templates.pug

      //- 공유 검색어가 있을 경우 data attribute로 전달
      if typeof sharedQuery !== 'undefined' && sharedQuery
        #shared-query(data-shared-query=sharedQuery)
